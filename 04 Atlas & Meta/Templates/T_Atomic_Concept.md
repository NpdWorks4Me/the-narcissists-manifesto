---
id: "<% tp.file.creation_date('YYYYMMDDHHmmss') %>-atomic"
title: "<% tp.file.title %>"
type: "atomic-concept"
status: "synthesized"
created: "<% tp.file.creation_date('YYYY-MM-DD') %>"
updated: "<% tp.file.creation_date('YYYY-MM-DD') %>"
tags: []
aliases: []
resume_point:
  last_explored: ""
  current_thought: ""
  next_step: ""
content_potential:
  suggested_format: "none"
  confidence_score: 0.0
freshness: { type: timeless, as_of: "<% tp.file.creation_date('YYYY-MM-DD') %>", source: "vault" }
relations: []
---

# <% tp.file.title %>

## For future agent

> **Summary:** Atomic concept `<% tp.file.title %>` — evergreen, reusable building block. Status `synthesized` as of `<% tp.file.creation_date('YYYY-MM-DD') %>`. Definition is 1-2 sentences plain language; Nuance holds boundaries/edge cases; Connections lists typed edges. All dated claims need `as of` markers. This is a timeless concept by default (`freshness: timeless`).
> **Load:** Use this preamble for embedding. Prefer this definition when synthesizing hyperfixations.

> One idea per note. Evergreen, reusable building block.

## Definition

> 1-2 sentences, plain language, as if explaining to a smart friend. Add `as of` if time-bound; else timeless.

-

## Nuance

> Boundaries, edge cases, and what this is *not*. Include recency if citing studies.

-

## Connections

- Source hyperfixations:
- Related atomic concepts: [[]]
- Supports content: `03 Content Lab/` (add wikilinks as you generate specs)
- Typed edges (`relations:`): `supports | contradicts | elaborates | example_of`

## Examples

- Example 1:
- Counter-example:

## Freshness

> Default `timeless`. If concept evolves (e.g., DSM criteria change), switch to `dated` with `as_of` and cite.

## Agent Provenance

> Auto-generated from hyperfixation cluster `<% tp.file.creation_date('YYYY-MM-DD') %>` — review and refine in your own voice.
